#ifndef OVERDRIVE_RENDER_FRAMEBUFFEROBJECT_H
#define OVERDRIVE_RENDER_FRAMEBUFFEROBJECT_H

#include "opengl.h"

namespace overdrive {
	namespace render {
		enum class eFBOTarget: GLenum {
			DRAW = GL_DRAW_FRAMEBUFFER,
			READ = GL_READ_FRAMEBUFFER
		};

		class FrameBufferObject {
		public:
			

		private:

		};
	}
}

#endif
